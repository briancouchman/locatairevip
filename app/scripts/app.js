'use strict';

define(['angular'], function (angular) {
		return angular.module('showcase', []);
});

/*
define(['facebook'], function(){
  console.log(FB);
  FB.init({
    appId      : 'YOUR_APP_ID',
    channelUrl : '//yourdomain.com/channel.html'
  });
  FB.getLoginStatus(function(response) {
    console.log(response);
  });
});

*/